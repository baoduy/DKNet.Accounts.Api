name: Publish Docker Image

on:
  workflow_dispatch: {}
  push:
    branches: [ main ]

concurrency:
  # One run of this pipeline per branch at a time; a newer commit cancels the older in-flight run.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  packages: write

env:
  Project_Path: 'ApiEndpoints/DKNet.Accounts.Api/DKNet.Accounts.Api.csproj'
  Client_Project_Path: 'ApiEndpoints/DKNet.Accounts.Client/DKNet.Accounts.Client.csproj'
  Image_Repository: 'baoduy/dknet.accounts-api'
  Console_Image_Repository: 'baoduy/dknet.accounts-console'
  Dotnet_Version: 10.x

jobs:
  publish-image:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.Dotnet_Version }}

      - name: Calculate version
        id: version
        uses: paulhatch/semantic-version@v6.0.2
        with:
          tag_prefix: "v"
          major_pattern: "(MAJOR)"
          minor_pattern: "(MINOR)"
          version_format: "${major}.${minor}.${patch}"
          bump_each_commit: false
          search_commit_body: false

      - name: Set NEXT_VERSION
        run: echo "NEXT_VERSION=${{ steps.version.outputs.version }}" >> $GITHUB_ENV

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # The .NET SDK builds and pushes the image itself (container properties live
      # in the csproj), so there is no Dockerfile to keep in sync.
      # ContainerRuntimeIdentifiers replaces --runtime: the SDK builds one image per RID
      # and pushes a manifest list, so each tag serves amd64 and arm64 hosts.
      - name: Build & push image
        run: |
          dotnet publish ${{ env.Project_Path }} \
            --configuration Release \
            --self-contained false \
            /t:PublishContainer \
            -p:ContainerRuntimeIdentifiers='"linux-x64;linux-arm64"' \
            -p:ContainerRegistry=ghcr.io \
            -p:ContainerRepository=${{ env.Image_Repository }} \
            -p:ContainerImageTags='"${{ env.NEXT_VERSION }};latest"'

      - name: Build & push console image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ui/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ env.Console_Image_Repository }}:${{ env.NEXT_VERSION }}
            ghcr.io/${{ env.Console_Image_Repository }}:latest

      - name: Pack NuGet package
        run: |
          dotnet pack ${{ env.Client_Project_Path }} \
            --configuration Release \
            -p:PackageVersion=${{ env.NEXT_VERSION }} \
            -o ./nupkgs

      # dotnet nuget push auto-pushes any .snupkg collocated with a matching .nupkg,
      # so this also ships the symbol package produced by the project's settings.
      - name: Push NuGet package to GitHub Packages
        run: |
          dotnet nuget push "./nupkgs/*.nupkg" \
            --source https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json \
            --api-key ${{ secrets.GITHUB_TOKEN }} \
            --skip-duplicate

      - name: Release
        uses: softprops/action-gh-release@v3.0.1
        continue-on-error: true
        with:
          generate_release_notes: true
          make_latest: true
          tag_name: v${{ env.NEXT_VERSION }}
        env:
          GITHUB_TOKEN: ${{ github.token }}

      - name: Summary
        run: |
          echo "Pushed ghcr.io/${{ env.Image_Repository }}:${{ env.NEXT_VERSION }} and :latest" >> $GITHUB_STEP_SUMMARY
          echo "Pushed ghcr.io/${{ env.Console_Image_Repository }}:${{ env.NEXT_VERSION }} and :latest" >> $GITHUB_STEP_SUMMARY
          echo "Pushed DKNet.Accounts.Client ${{ env.NEXT_VERSION }} to GitHub Packages" >> $GITHUB_STEP_SUMMARY
