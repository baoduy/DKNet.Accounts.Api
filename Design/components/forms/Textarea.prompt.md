Multi-line text, same shell as `Input`.

```jsx
<Textarea rows={3} placeholder="Description" value={description} onChange={onDescription} />
```

Vertical resize only. Keep to three rows by default — descriptions on this console are one line in practice.
