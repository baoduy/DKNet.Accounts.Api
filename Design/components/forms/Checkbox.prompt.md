A square checkbox with an inline label.

```jsx
<Checkbox checked={permitted} onChange={onPermitted} label="Permitted to go negative" />
```

16px, `radius-xs`, filled `--primary` when checked. The label is part of the hit target.
