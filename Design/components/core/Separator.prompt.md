A 1px `--border` hairline.

```jsx
<Separator />
<Separator orientation="vertical" />
```

The vertical form divides the inline balance strip above a statement. Prefer a border on the element itself when one already exists; use Separator when nothing else owns the edge.
