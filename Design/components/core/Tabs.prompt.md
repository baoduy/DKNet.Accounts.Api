The console's only tab treatment: a row of chips, the selected one on the accent ground at weight 600.

```jsx
<Tabs items={['Single','Batch']} value={mode} onChange={setMode} />
<Tabs items={['Credit','Debit']} value={direction} onChange={setDirection} />
```

There is no underline or boxed tab bar anywhere in the design. Keep tab sets to two or three short words.
